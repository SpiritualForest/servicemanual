## Better documentation will be supplied later. For now:

### Install Maven, run "mvn install" to install the dependencies, then run "mvn spring-boot: run".

### API endpoints

## Supported HTTP methods: GET, POST, PUT, DELETE
## /tasks/\<id>
